spring:
  profiles:
    active: mongo # Valor por defecto

# --- Este bloque solo se activa si el perfil es 'relational' ---
---
spring:
  config:
    activate:
      on-profile: relational
  datasource:
    url: jdbc:postgresql://localhost:5433/biblioteca_db
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

# --- Este bloque solo se activa si el perfil es 'mongo' ---
---
spring:
  config:
    activate:
      on-profile: mongo
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb+srv://castillejoangulo:Vien2023@cluster0.0cw5yye.mongodb.net/?appName=Cluster0}
      database: biblioteca_db

server:
  port: ${PORT:8080}
  ssl:
    enabled: ${SSL_ENABLED:false}